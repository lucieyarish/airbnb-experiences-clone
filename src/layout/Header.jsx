import NavBar from '../components/NavBar';
import Hero from '../components/Hero';

const Header = () => {
  return (
    <header>
      <NavBar />
      <Hero />
    </header>
  );
};

export default Header;
